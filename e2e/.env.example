# ── E2E 测试专属变量（docker-native, profile=e2e）──────────
# 这些变量仅在 `docker compose --profile e2e run --rm e2e` 运行 e2e 容器时需要，
# 正常部署无需配置。
#
# 用法：把下列变量填好后【追加】到项目根目录的 .env.local——
# docker compose 通过 --env-file .env.local 对 ${E2E_*} 做插值，再注入 e2e 容器。
# 详见 README.md 的 "E2E 测试" 章节。

# 调用上游 new-api 的真实 API key（e2e 用它发起测试请求；在 new-api 侧创建一个可用 token）
E2E_API_KEY=

# new-api 中已配好的、供 e2e 使用的模型名（需与 new-api 后台配置一致）
E2E_OPENAI_MODEL=gpt-5.4
E2E_CLAUDE_MODEL=claude-sonnet-4-6
